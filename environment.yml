# environment.yml
name: crop-disease-prediction
dependencies:
  - python=3.10
  - pip
  - pip:
    - streamlit==1.51.0
    - tensorflow
    - numpy          # Use latest compatible version
    - pillow
    # Add any other missing libraries from your original requirements.txt here
